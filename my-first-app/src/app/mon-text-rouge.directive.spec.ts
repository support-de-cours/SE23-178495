import { MonTextRougeDirective } from './mon-text-rouge.directive';

describe('MonTextRougeDirective', () => {
  it('should create an instance', () => {
    const directive = new MonTextRougeDirective();
    expect(directive).toBeTruthy();
  });
});
